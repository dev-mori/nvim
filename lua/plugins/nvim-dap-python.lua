return {
  -- "mfussenegger/nvim-dap-python",
  -- config = function()
  --   local handle = io.popen("poetry run which python")
  --   local poetry_python_env_path = handle:read("*a"):gsub("%s+", "")
  --   handle:close()
  --   require("dap-python").setup(poetry_python_env_path)
  -- end,
}
